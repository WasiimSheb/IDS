from collections import defaultdict

flows = defaultdict(dict)  # The shared dictionary for tracking network flows.

# DIR = '/app/db/'  # The directory where the SQLite database is stored for dockers.

DIR = 'Database/'  # The directory where the SQLite database is stored for local machines.